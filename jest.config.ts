// NOTE: superseded by jest.config.js (Jest prefers .js over .ts for config
// resolution, and loading a .ts config directly requires `ts-node`, which
// this project does not depend on). Kept as a no-op export rather than
// deleted so this doesn't look like an accidental leftover.
export {};
